<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit Post - Instagram</title>
  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; }
    html, body { height: 100%; }
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial;
      background: #000; color: #f1f1f1;
      display: flex; justify-content: center; padding: 24px 12px;
    }
    .container {
      width: 100%; max-width: 600px;
      background: #121212; border: 1px solid #262626;
      border-radius: 8px; padding: 24px; box-shadow: 0 1px 0 rgba(255,255,255,0.05);
    }
    header {
      display: flex; align-items: center; justify-content: space-between;
      border-bottom: 1px solid #262626; padding-bottom: 12px; margin-bottom: 24px;
    }
    .logo { display: flex; align-items: center; gap: 10px; }
    .logo-img { height: 32px; width: auto; border-radius: 6px; }
    .logo h1 { font-size: 20px; color: #fff; }
    form { display: flex; flex-direction: column; gap: 18px; }
    label { font-size: 14px; font-weight: 600; color: #ccc; margin-bottom: 4px; display: inline-block; }
    input, textarea {
      width: 100%; padding: 10px 12px; border-radius: 6px;
      border: 1px solid #333; background: #181818; color: #fff;
      font-size: 15px;
    }
    textarea { resize: none; height: 100px; line-height: 1.4; }
    input:focus, textarea:focus { outline: none; border-color: #555; }
    .btn-row {
      display: flex; justify-content: space-between; align-items: center; margin-top: 8px;
    }
    .btn {
      background: #0095f6; color: #fff; border: none;
      padding: 10px 18px; border-radius: 6px;
      font-size: 15px; font-weight: 600; cursor: pointer;
      transition: background 0.2s ease;
    }
    .btn:hover { background: #0077cc; }
    a.cancel { color: #999; text-decoration: none; font-size: 14px; }
    a.cancel:hover { text-decoration: underline; }
  </style>
</head>

<body>
  <div class="container">
    <header>
      <div class="logo">
        <img src="/instagram.png" alt="Instagram Logo" class="logo-img" />
        <h1>Edit Post</h1>
      </div>
      <a href="/" class="cancel">Back</a>
    </header>

    <form action="/index/<%= post.id %>?_method=PUT" method="POST">
      <div>
        <label for="title">Title</label>
        <input type="text" id="title" name="title" value="<%= post.title %>" required />
      </div>

      <div>
        <label for="caption">Caption</label>
        <textarea id="caption" name="caption" required><%= post.caption %></textarea>
      </div>

      <div class="btn-row">
        <a href="/" class="cancel">Cancel</a>
        <button type="submit" class="btn">Update</button>
      </div>
    </form>
  </div>
</body>

</html>
